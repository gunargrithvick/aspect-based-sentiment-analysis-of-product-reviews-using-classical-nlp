[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
# CLI scripts and the Vercel entrypoint add src/ to sys.path before importing
# the local package. E402 is therefore intentional in those files.
ignore = ["E402"]
